diff --git a/utils/Makefile.in b/utils/Makefile.in
index 210828a..062c4e2 100644
--- a/utils/Makefile.in
+++ b/utils/Makefile.in
@@ -6,7 +6,7 @@ static:
 
 install:
 	chmod 755 lxrad-config
-	${CP} -v lxrad-config ${bindir}
+	${CP} -v lxrad-config ${bindir}/
 
 uninstall:
 	${RM} ${bindir}/lxrad-config
